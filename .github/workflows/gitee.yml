name: Mirror to Gitee and Build Gitee Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  sync:
    uses: yilunyuwan/test-rui/.github/workflows/mirror-to-gitee.yml@master
    secrets: inherit

#        uses: Yikun/hub-mirror-action@master
#        with:
#            src: github/yilunyuwan
#            dst: gitee/yilunyuwan
#            dst_key: ${{ secrets.GITEE_PRIVATE_KEY }}
#            dst_token: ${{ secrets.GITEE_TOKEN }}
#            static_list: "test-rui"

